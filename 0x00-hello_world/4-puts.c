#include <stdio.h>

	/**
	 * main is where the execution begins
	 * 
	 * 
	 *
	 * Retun is o when the program executes
	 *
	 */

int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
