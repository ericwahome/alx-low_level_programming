#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/* 
*Main:Starts executing
*
*Return-The retun value is zero
 */
int main(void)
{
	char lc;
	srand(time(0));
	n = rand() - RAND_MAX / 2;
	for (lc="a"; lc<"z"; lc++)}
	putchar(lc);
{
	return (0);
}
