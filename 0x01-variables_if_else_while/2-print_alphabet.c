#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main:Starts here
 *
 * Return:Return value is 0 every time it runs
 */
int main(void)
{
char lc;
for (lc = 'a'; lc <= 'z'; lc++)
putchar(lc);
putchar('\n');
return (0);
}

