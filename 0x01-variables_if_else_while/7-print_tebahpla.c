#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 *main-Starts here
 *
 *Return:The return value is 0 every time it runs
 */
int main(void)
{
	char lc;
for (lc = 'z'; lc >= 'a'; lc--)
		putchar(lc);
		putchar('\n');
	return (0);
}
