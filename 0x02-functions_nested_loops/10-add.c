#include "main.h"
/**
 * add - adds two integers and returns the result
 * @fst: first number input
 * @scd: second number input
 * Return: the sum of the two
 */
int add(int fst, int scd)
{
	return (fst + scd);
}
